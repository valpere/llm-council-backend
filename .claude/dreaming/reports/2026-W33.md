# vmm-rada dreaming pass — 2026-W34

Date: 2026-08-16
Branch surveyed: main
Commits examined: ~46 since 2026-05-17 (last 90 days); 22 in the focused 30-day window
PRs examined: 15 merged (most recent: #343 → #317, plus 5+ older via persisted gh output)

## TL;DR

1. **Frontend-extraction drift is now clean.** The 15-file sweep (#333/#341), the residual sweep (#343), and the `ship`/`fix-review`/`code-generator`/`code-simplifier`/`tech-lead`/`find-bugs`/`improve`/`revival`/`housekeeping` SKILL.md cleanups have eliminated all in-repo frontend references. No new violations in the last 14 days. `apply-dreaming` re-verified only 2 legitimate mentions remain.
2. **Docs-triad drift remains the #1 recurring review theme.** Two separate PRs in this window (#342, #327) were either creating or fixing docs-triad sync. The new `docs-triad-sync-gate` rule in `tech-lead.md` (PR #342) is the formal response — but it's not yet wired into the `context-essentials.md` file that gets re-injected on compaction. Recommendation: mirror the gate in `context-essentials.md` so it survives summarization.
3. **Plan hygiene is healthy.** `.claude/plans/` contains only `README.md` (the convention doc) — no orphan plan files older than 14 days. Workflow is being followed.
4. **Agent-memory growth is healthy but top-heavy.** 23/24 memories were touched in the last 2 days (W32/W33 dreaming + the W33 sweep); the four older ones (77–78d, May/June 2026) are all still relevant and were explicitly re-referenced in W33 work. No contradictions with `context-essentials` spotted.

## 1. Context-essentials drift

| Rule | Evidence | Severity | Suggest |
|------|----------|----------|---------|
| "Frontend was removed (2026-07-19)" | `git grep -l frontend` over `.claude/skills/*/SKILL.md` returns **no matches** after #333/#341/#343. Two legitimate mentions per `apply-dreaming` audit (`.github/instructions/frontend.instructions.md` is a GitHub Copilot instruction file, dormant; the dreaming reports themselves). | **low** (clean) | None — rule is being followed. |
| "No `--no-verify`" | `git log --all --since=90d --grep='--no-verify'` → empty. `git log --all --since=90d --grep='no-verify'` → empty. | **low** (clean) | None. |
| "No `fmt.Println` in `cmd/`" | `Grep fmt\.Println cmd/` → no matches in 2026-08 code. (Pre-PR `cmd/server/main.go` had it; replaced long ago.) | **low** (clean) | None. |
| "Docs-triad drift is the most common review comment" | `context-essentials.md:40-42` lists it as a known theme (W19 §2). PR #342 (2b76820) **just added** the same gate as a Tech Lead review criterion. PR #327 (b1d1186) was a docs-triad fix (`user-guide.md`, `README.md` drift from YAML config). | **medium** | The new criterion lives in `.claude/agents/tech-lead.md` only. It should be mirrored into `context-essentials.md` so it survives compaction. Confidence: **high**. |
| "Plans deleted after issue creation" | `ls .claude/plans/` → only `README.md` (no orphan plans). | **low** (clean) | None. |
| "Workflow: … `/fix-review` …" | Confirmed: every merged PR #317–#343 carried a `/fix-review` comment with parallel-pass + Claude arbiter structure. | **low** (clean) | None. |
| "Stack: Go 1.26.5" | Not re-verified this pass; last bumped in ca083f5 (Go 1.26.4). 1.26.5 referenced in CLAUDE.md and tech-lead/project_architecture.md (2d old). | n/a | Out of scope of this pass. |

## 2. Recurring `/fix-review` themes

Counted across the 15 PRs in the persisted `gh pr list --state merged --limit 20` output (#317–#343, plus #327–#329 from late July):

| Theme | Count | PRs | Suggest |
|-------|-------|-----|---------|
| **Docs drift between `CLAUDE.md`/`architecture-v2.md`/`strategies.md`** | explicit gate added this window; 1+ prior fix PR (#327 user-guide/README drift, #318 openapi cross-refs, #319 stale `/plan` + Copilot references). | #342 (gate), #327, #318, #319 | ✅ Already addressed structurally via PR #342. Now needs the mirror in `context-essentials.md` (see §1). Confidence: **high**. |
| **Pointer-to-nonexistent-file / stale pointer in agent-memory** | n=2 confirmed this window. #339: pointer to `memory-rename-reverify-gate.md` from a different branch (cross-branch contamination from concurrent subagent). #340: `How to apply` text contradicting the section above it (mid-edit oversight). | #339, #340 | New rule added in #339 → `tech-lead/review-verify-checked-out-branch.md` (1d old). The #340 self-contradiction case isn't yet captured. Suggest: add a short tech-lead memory note: "when rewriting a memory's body, scan the closing `**Why:**`/`**How to apply:**` block to match — same author, same PR, often the trailing lines drift from the rewritten section." Confidence: **medium**. |
| **Out-of-scope findings flagged within approved-scope PR** | n=2 dismissed-but-flagged (#341, #329) where reviewers surfaced legitimate findings the PR wasn't approved to fix. | #341, #329 | Already handled by the workflow (flagged for separate user confirmation). No new rule needed. |
| **False-positive class for shell/bash patterns** | n=4 dismissed (#339, #328, #329, #342) — model misreads of bash `${var:+...}`, `eval`, `</dev/null`, etc. | #339, #328, #329, #342 | ✅ Already addressed via `docs(fix-review): arbiter notes — shell false-positive class` (7c74e32 / PR #339). |
| **Pre-existing pattern defense** ("this is in the canonical shared file, not introduced here") | n=3 (#328, #329, #325) — recurring arbiter ruling pattern. | #328, #329, #325 | Could be promoted to a memory note for the arbiter (which has no agent file of its own). Confidence: **low** (the pattern is already working — promote only if it appears in a 4th distinct PR). |
| **Strategy-registration gap (silent un-registerable 8th strategy)** | n=1 confirmed this window (#323 added `TestShippedConfigCoversAllStrategies` after RoleBased fell into this trap for months). | #323 | ✅ Fixed structurally. Memory lives in `rolebased-strategy-orphan.md` (2d old). |
| **Test for the new primary config path missing** | n=1 confirmed (#323 — YAML coverage test). | #323 | One-off, no rule needed. |

**Overall:** the recurring themes are converging toward "agent-memory and docs-triad drift" — both are now governed by the right processes; what's missing is mirroring them into `context-essentials.md`.

> [applied 2026-08-23: the "pointer-to-nonexistent-file / stale pointer in agent-memory" finding's #340 self-contradiction case — .claude/agent-memory/tech-lead/memory-edit-trailing-block-drift.md created (direct-edit, no gate needed, agent-local memory).]
> [planned 2026-08-23: the docs-triad-sync-gate mirror into context-essentials.md — .claude/plans/1-dreaming-w33-context-essentials-docs-triad-mirror.md created; awaiting /backlog Tech Lead plan review (load-bearing file, plan-and-gate routing).]

Both findings surfaced only after `/fix-review` on the W34 triage PR (#345) flagged that this report had been committed unprocessed on the assumption it was fully superseded by W34 — it wasn't; these two items were real and W34 didn't cover them. Lesson: "superseded by a later report" is not the same as "every finding in the earlier report is subsumed" — verify per-finding, not just per-report.

## 3. Stale plans

- `.claude/plans/` contains **only** `README.md` (4.0K). No orphan plans. Workflow is being followed cleanly.
- Most recent plan-creation touch (visible in `git log -- .claude/plans/`) was #4accf48 (2026-07-28), which **fixed** the README example rather than creating a plan.

**Suggestion:** none — current state is exemplary.

## 4. Agent-memory health

Computed mtimes today (2026-08-16):

```
  1d  frontend-extraction-stale-prompts.md
 77d  cors-allowed-methods.md             ← oldest, still referenced
  2d  MEMORY.md
 22d  stage0-done-not-on-wire.md
 78d  error-status-mapping.md             ← oldest, still referenced
  2d  rolebased-strategy-orphan.md
 77d  storage-title-handling.md           ← oldest, still referenced
 78d  usage-cost-aggregation.md           ← oldest, still referenced
  2d  frontend-prune-plan-corrections.md
  2d  tech-lead/project_architecture.md
  1d  tech-lead/prune-by-deletion-not-rewording.md
  1d  tech-lead/governance-enforcement-point.md
  1d  tech-lead/skill-doc-drift-fixreview.md
  2d  tech-lead/known_issues.md
  1d  tech-lead/MEMORY.md
  1d  tech-lead/docs-triad-sync-gate.md
  2d  tech-lead/context-essentials-inclusion-test.md
  1d  tech-lead/review-verify-checked-out-branch.md
  1d  tech-lead/memory-rename-reverify-gate.md
  1d  tech-lead/review-criteria-need-output-slot.md
  2d  tech-lead/agent-scope-boundary-pairs.md
  2d  code-generator/MEMORY.md
  1d  go-security-reviewer/MEMORY.md
  1d  go-security-reviewer/backend_security_posture.md
  2d  code-simplifier/MEMORY.md
```

- **Stale (>60d):** 4 files — `cors-allowed-methods.md` (77d), `error-status-mapping.md` (78d), `storage-title-handling.md` (77d), `usage-cost-aggregation.md` (78d). All were touched in the W33 sweep (or referenced by it). All are still semantically current and were cited in recent PRs. **No action needed.**
- **Contradictions with context-essentials:** none spotted on this pass. The `stage0-done-not-on-wire.md` memory correctly tracks the SSE wire shape that `context-essentials.md` doesn't enumerate (which is fine — it's an implementation detail).
- **Duplicates across agents:** none. Each memory is agent-specific or shared top-level only.
- **Newly bootstrapped:** `code-generator/MEMORY.md` and `code-simplifier/MEMORY.md` (W32 §4 M1, 2d old). Both are intentional bootstrap shells — no memories yet, by design.

**Suggestion:** none — memory state is healthy. The 1d-old burst is the W33 sweep landing (PRs #339–#343).

## 5. Skill / agent inventory

**Skills (`.claude/skills/`):** `apply-dreaming/`, `backlog/`, `doubt-driven-development/`, `find-bugs/`, `fix-review/`, `housekeeping/`, `improve/`, `lib/`, `references/`, `revival/`, `self-learn/`, `session-end/`, `ship/`.

- **`apply-dreaming/`** — user-level only (per global CLAUDE.md). 0 hits in 90d commit log because it runs at user-level scope.
- **`doubt-driven-development/`** — last touched 2026-06 (3a880e7). 90d window: silent. Confidence: **low** that it's unused — `doubt-driven-development` is a meta-cognitive skill invoked on demand.
- **`housekeeping/`** — last touched in the W33 sweep (e087aa6). Clean.
- **`improve/`** — last touched in the W33 sweep (e087aa6). Clean.
- **`revival/`** — last touched in the W33 sweep (e087aa6). Clean.
- **`find-bugs/`** — not directly visible in commit log; invoked via slash command.

**Agents (`.claude/agents/`):** 10 agents (bug-fixer, ci-build-agent, code-generator, code-simplifier, docs-maintainer, go-security-reviewer, pm-issue-writer, static-analysis, tech-lead, test-generator).

- **`security-reviewer` retired 2026-08-14** (PR #340, `afa3b0d`). Cross-refs in `go-security-reviewer.md` cleaned up. New agent-memory entry: `tech-lead/agent-scope-boundary-pairs.md` (1d).
- **No overlapping agent roles.** Each agent has a clear scope per `CLAUDE.md`'s skill table.

**Suggestion:** none — inventory is lean and clean.

## 6. Patterns I noticed

1. **The `context-essentials.md` file has become asymmetric with `tech-lead.md`.** The latter has acquired multiple new criteria this window (docs-triad-sync-gate, review-verify-checked-out-branch, review-criteria-need-output-slot, governance-enforcement-point, prune-by-deletion-not-rewording) but none of them are mirrored back into `context-essentials.md`. The 60-line budget on `context-essentials.md` is the limiting factor; a "Tech Lead review criteria are canonical — see `.claude/agents/tech-lead.md`" pointer would let new criteria propagate without bloating the re-injection file.

2. **W33 (this window) was a "meta-pass" week.** 8 of the 22 commits in the 30-day window are `chore(agents):` / `docs:` — almost no production code landed. This is healthy: it's the cleanup wave after the RoleBased/YAML-council-config burst in W30–W31, plus the security-reviewer retirement. Expect production code cadence to resume in W34/W35.

3. **`/apply-dreaming` is operating cleanly.** Its W33 output (the two remaining legitimate frontend mentions) was correctly identified, and the sweep that followed (PRs #333/#341/#343) closed it out. The 2-line residual is the GitHub Copilot `.github/instructions/frontend.instructions.md` and the dreaming-report archives — both are correctly out of scope.

4. **The arbiter's "shell false-positive" rule from PR #339 is paying off.** Three of the dismissed findings this window were the same class (`${var:+...}`, `eval`, `</dev/null` misinterpretation). The arbiter consistently dismisses them now with a one-line explanation rather than getting snagged — a real efficiency win.

5. **No reverted commits in the 90-day window.** `git log --all --since=90d | grep -i revert` returns nothing. The team's review gate is preventing bad merges from landing.

## 7. What I couldn't tell (need manual review)

- **Whether the new docs-triad-sync-gate from #342 is being followed in practice on every PR.** I saw it added to `tech-lead.md`, but only 1 PR (#343) has merged since, and it was a deletion sweep where the gate trivially passes. The next substantive PR touching docs will be the real test.
- **Whether `cors-allowed-methods.md`, `error-status-mapping.md`, `storage-title-handling.md`, `usage-cost-aggregation.md` (77–78d old) still reflect the live code.** They were referenced in recent PRs but never re-verified end-to-end. A quick spot-check against `internal/storage/storage.go`, `internal/council/types.go`, and the gateway error mapping would close this.
- **Whether `go-security-reviewer/backend_security_posture.md`'s "CVE (rolling target, see file for current CVE ID)" pointer is still current.** The Go toolchain was bumped to 1.26.4 (ca083f5) and CLAUDE.md says 1.26.5 — there may be a newer govulncheck CVE on the radar. The memory file was last touched 1d ago (W33 sweep), so this might already be addressed; without reading the file I can't confirm.
- **The current state of the W33 follow-ups flagged in PR #341** ("plans/README.md `frontend` enum" and "dead `eslint-fix.cjs` hook") — these were explicitly deferred for separate user confirmation per the arbiter's dismissal. Whether they were handled in #343 or are still open is unclear from this pass.

## 8. Recommended next actions (ranked)

1. **Mirror `docs-triad-sync-gate` from `tech-lead.md` into `context-essentials.md`** — single line, high value (survives compaction).
2. **Add an arbiter-pattern memory note for "when rewriting a memory body, re-read the trailing `**Why:**`/`**How to apply:**`"** — addresses the #340 self-contradiction class.
3. **Spot-check the 4 oldest (77–78d) agent memories against current code** to confirm they didn't rot silently during the W30–W32 burst.
4. **Resolve the W33 deferred items from PR #341** (plans/README `frontend` enum, dead `eslint-fix.cjs` hook) in a separate confirmed PR — they're tracked but not closed.
