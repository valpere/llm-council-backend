Dreaming-pass work is curation, not code exploration — graphify is for code navigation and doesn't apply to `.claude/` artifact review. I have sufficient data: context-essentials read, 90-day commit log, 20 merged PRs with full `/fix-review` verdicts, plans/skills/agents/agent-memory inventory, banned-pattern grep results. No task list needed — this is a single sequential read-only analysis. Writing the report.

# vmm-rada dreaming pass — 2026-W34

Date: 2026-08-23
Branch surveyed: main
Commits examined: ~80 (since 2026-05-25, focus 2026-08-13→2026-08-23)
PRs examined: 20 merged (most recent #317–#343)

## TL;DR

- **Project is in its cleanest state since the W19 dreaming pass.** All `frontend/` artifacts have been swept from `.claude/`, no banned-pattern violations, no `fmt.Println` in `cmd/`, no `--no-verify` commits in the 90-day window.
- **A 0-byte report stub `2026-W34.md` exists** alongside the W33 report — the W34 dreaming pass is happening now (this report). No duplicate or stale pass files to clean up.
- **The two recurring `/fix-review` themes worth promoting into context-essentials are both already covered** by recent commits: docs-triad sync (rule added in 2b76820 / #342) and "vote count ≠ verdict" (covered by the W19 fix-review canon + memory-rename-reverify-gate.md). Nothing new to add.
- **No stale plans** — `.claude/plans/` contains only `README.md` (the lifecycle doc itself), as the W31 and W33 passes already confirmed.
- **One thing that may need attention next session:** `sessions.db` is now 131.6 MB (was small enough to be ignored in earlier passes). Not a rules violation, but worth noting before it starts blocking tool calls.

## 1. Context-essentials drift

Cross-referenced each rule in `.claude/context-essentials.md` against the last 30 days of commits and 20 most recent PRs.

### "Frontend was removed from this repo (2026-07-19)"

- Evidence: PRs #333, #341, #343 (commits e087aa6, 6271b72) are the explicit "remove remaining frontend artifacts" sweep. The 15-file sweep (e087aa6) was followed by the residual sweep (6271b72) after the same pass found two more lingering mentions in `frontend-extraction-stale-prompts.md` (plans/README.md `frontend` enum + dead `eslint-fix.cjs` hook) — those are now flagged as out-of-scope DEFER items pending a separate `.claude/` change-governance confirmation, not silently bundled. PRs #341 / #343 both passed `/fix-review` with all flagged items dismissed (out-of-scope, not new violations).
- Severity: **none** (rule intact)
- Suggest: no change. The state is clean and the deferred items are properly tracked in PR bodies, not in the code.

### "Backend: Go. Run `go build`, `go vet`, `go test ./...` before `/ship`."

- Evidence: Every PR #317–#343 with code changes (not pure docs) explicitly reports `go test -race -count=1 ./...` results in its `/fix-review` body. Counts climb naturally: 356 → 363 → 378 tests as new ones land. No CI-failure mentions, no reverts.
- Severity: **none** (rule intact)
- Suggest: no change.

### "Plans live in `.claude/plans/` … delete the plan file after issue creation."

- Evidence: `ls .claude/plans/` returns only `README.md` (the lifecycle doc) — zero active plan files. Confirmed across W31, W32, W33 dreaming passes; still true today.
- Severity: **none** (rule intact)
- Suggest: no change. This is a sustained clean streak — consider noting in W35 if any plan lingers, but right now the rule is being followed religiously.

### "Tech Lead approval is the gate before any code generation."

- Evidence: Cannot directly verify from git log (no commit metadata captures "Tech Lead approved" beyond agent self-reports), but every PR in the W19–W34 window references the planning gate in its body or in linked issues. Tech-lead agent's own audit (e087aa6 sweep) is the W33 proof-of-process.
- Severity: **none** (rule intact, no evidence of bypass)
- Suggest: no change.

### "PRs are squash-merged. Never merge commits or rebase-merge."

- Evidence: `gh pr list --state merged --limit 20` shows all 20 PRs merged with single squash commits (matches the W19-era workflow lock-in).
- Severity: **none**
- Suggest: no change.

### "Mark planned vs current explicitly … never write future-tense behaviour as if it were current."

- Evidence: The "PLANNED:" / "NOT YET WIRED:" convention has been used consistently since the W19 pass introduced it as a fix-review response. PRs #319, #326, #327, #330, #338 all cite this rule in their verification bodies.
- Severity: **none**
- Suggest: no change. Already a reflex.

### "Update `CLAUDE.md`, `architecture-v2.md`, `strategies.md` together when a feature lands."

- Evidence: Commit 2b76820 / PR #342 (chore(tech-lead): add docs-triad sync gate to plan + code review criteria) just added this exact triad-sync requirement as an explicit Tech-Lead review criterion, *and* as an explicit plan-completion gate. PR #342 itself passed `/fix-review` (1 dismissed false positive). The rule is now enforced at the plan and review gates, not just expected.
- Severity: **none** (rule just got stronger)
- Suggest: no change. This is the kind of promotion-from-theme into-enforcement that previous dreaming passes recommended; it's now done.

### "No `--no-verify` on git operations."

- Evidence: `grep -rn "no-verify\|--no-verify" .claude/ docs/ CLAUDE.md` returns matches only inside dreaming reports (where the rule is documented) and inside the dreaming prompt template. No commit message or shell history shows a real `--no-verify` invocation. Holds since W19.
- Severity: **none**
- Suggest: no change.

### "No `fmt.Println` in `cmd/` packages — use the configured logger."

- Evidence: `grep -rn "fmt.Println" cmd/` returns **zero matches** in the 2026-08-23 working tree. Holds since W19.
- Severity: **none**
- Suggest: no change.

## 2. Recurring `/fix-review` themes

Reviewed all 20 `/fix-review` summaries in the merged PR list. Themes and their vote-tally distribution:

### Theme: "Self-contradiction in edited-but-not-fully-rewritten content"

- Hits: 2 (#340, #324)
  - #340: 1 finding, 1 confirmed — `backend_security_posture.md:37` "How to apply" still said "CSP gap … is real and unfixed" after the section above it was rewritten to "Resolved (security headers)". Self-caught mid-edit.
  - #324: 1 finding, 1 confirmed — `dreaming.sh` self-fix with `grep -q '^['` (an unterminated bracket expression in POSIX/BRE). Caught by writing a 4-case test harness.
- Suggest: This is the "verify-before-done" pattern that the tech-lead agent's `memory-rename-reverify-gate.md` already documents (PR #339 explicitly added it). No new rule needed — the pattern is now well-internalized.
- Confidence: **high** (rule exists, recent PRs cite it in their fix-review bodies).

### Theme: "Out-of-scope findings bundled into a sweep PR"

- Hits: 2 (#341, #343, partially #329)
  - #341: 2 of 2 dismissed as out-of-scope — flagged for separate confirmation per `.claude/` change-governance rule.
  - #343: 0 findings, but the entire PR exists because #341 *didn't* bundle them.
- Suggest: This is a healthy artifact of the explicit rule "never modify `.claude/` without approval" — the rule is working. The deferred items are tracked in PR bodies. No new rule needed; possibly a "tracked-deferred-items" index in `.claude/agent-memory/MEMORY.md` would help surface them across passes, but that's a docs-debt call, not a context-essentials addition.
- Confidence: **high** (working as designed).

### Theme: "Model-arbiter false positives on bash idioms"

- Hits: 3 (#328, #329, #339)
  - #328: `</dev/null` "clobbers" stdin — empirically verified false.
  - #328: `${model:+--model "$model"}` "bare flag" — misread of bash param expansion.
  - #329: `2>/dev/null` swallows stderr — pre-existing family pattern.
- Suggest: Already in the fix-review canon as "verify-before-done" / "shell false-positive class" (per #339's own summary: "arbiter notes — shell false-positive class + verify-before-done pattern"). The arbiter is rejecting these correctly every time. No new rule; existing pattern is the rule.
- Confidence: **high** (working).

### Theme: "Missing regression test for a new code path"

- Hits: 1 (#323 — confirmed) — `TestAllStrategiesRegisteredOrExempted` only checked env-var fallback; new `TestShippedConfigCoversAllStrategies` added to cover YAML-primary path (preventing a repeat of the RoleBased-unregistered-for-months issue, PR #302).
- Suggest: This is a *positive* theme — exactly the kind of "every new path gets a test that catches the gap the new path fills" thinking. Already covered by the global `feedback_test_first_discipline` rule. No new rule needed.
- Confidence: **high**.

### Theme: "YAML/registry coverage parity with code-defined Strategy enum"

- Single hit (#323), but the *fix* (a build-time invariant) is the kind of pattern that prevents future drift. Already documented in `rolebased-strategy-orphan.md` (W32 sweep).
- Suggest: no new rule; the test is the contract.
- Confidence: **high**.

## 3. Stale plans

- `ls .claude/plans/` returns only `README.md` (4.0K, the lifecycle doc) — zero active plan files.
- The W33 report (`2026-W33.md`) and the W32 report (`2026-W32.md`) both confirm this was already clean in their windows.
- The lifecycle doc itself has not been touched in this window beyond #326 (removed a dead `**Plan:**` footer reference) and #319 (compact + cross-refs).
- Suggest: no action. The rule is being followed perfectly.

## 4. Agent-memory health

Inventory (file mtime gives a sense of staleness, though all listed files have been touched within the W32–W33 sweep window):

- `.claude/agent-memory/code-generator/MEMORY.md` (393B) — small, recently refreshed.
- `.claude/agent-memory/code-simplifier/MEMORY.md` (401B) — small, recently refreshed.
- `.claude/agent-memory/go-security-reviewer/MEMORY.md` (303B) — small, recently refreshed.
- `.claude/agent-memory/tech-lead/MEMORY.md` (1.7K) — substantially larger; this agent has the most accumulated learnings, with 11 dedicated memory files plus 4 in the shared root.

Per-agent checks:

### tech-lead (11 dedicated memory files in `agent-memory/tech-lead/`)
- `agent-scope-boundary-pairs.md`, `context-essentials-inclusion-test.md`, `docs-triad-sync-gate.md`, `governance-enforcement-point.md`, `known_issues.md`, `memory-rename-reverify-gate.md`, `project_architecture.md`, `prune-by-deletion-not-rewording.md`, `review-criteria-need-output-slot.md`, `review-verify-checked-out-branch.md`, `skill-doc-drift-fixreview.md`
- All recently created/refreshed in the W32 sweep (PR #330). No obvious staleness.
- Contradictions with context-essentials: **none found** — `docs-triad-sync-gate.md` codifies the rule that's now in context-essentials; `prune-by-deletion-not-rewording.md` codifies the deprecation convention.
- Duplicates with other agents: **none obvious** — the tech-lead memories are scoped to its review/planning duties, not overlapping with code-generator or go-security-reviewer.

### go-security-reviewer
- `backend_security_posture.md` (one file in `agent-memory/root/`, plus MEMORY.md) — refreshed in PR #340 (CSP gap formally resolved; the file was rewritten with a "Resolved" note and PR #340 also caught the self-contradiction in the same file at line 37).
- Status: clean, recently corrected.

### code-generator / code-simplifier
- Both have minimal memories (MEMORY.md only, <500B each). Healthy "no accumulated drift" state.

### Shared agent-memory root
- `cors-allowed-methods.md`, `error-status-mapping.md`, `frontend-extraction-stale-prompts.md`, `frontend-prune-plan-corrections.md`, `rolebased-strategy-orphan.md`, `stage0-done-not-on-wire.md`, `storage-title-handling.md`, `usage-cost-aggregation.md`
- All 8 are topic-specific single-fact memories from W19–W32 sweeps. None duplicate each other. `frontend-extraction-stale-prompts.md` is the most-recently-touched (PR #341 sweep), and itself now documents the out-of-scope DEFER items that PR #341 couldn't bundle.

### Stale candidates (mtime > 60 days)
- **None.** The most-recent memories dominate; the deepest W19–W23 memories have all been either refreshed, retired (security-reviewer retirement in #340), or consolidated.

## 5. Skill / agent inventory

### Skills (`.claude/skills/`)
- `apply-dreaming/`, `backlog/`, `doubt-driven-development/`, `find-bugs/`, `fix-review/`, `housekeeping/`, `improve/`, `lib/`, `references/`, `revival/`, `self-learn/`, `session-end/`, `ship/`
- Cross-reference with recent activity:
  - `fix-review/` — heavy recent activity: #328 (external_agents tier 2), #329 (agy adapter), #339 (arbiter notes). Most active skill.
  - `ship/` — #338 (fix Step 7 dead-3-round-sequential-review docs drift). Just touched.
  - `backlog/` — uses plans/, which is empty. Quiet but expected (no plans = no backlog conversion right now).
  - `session-end/` — #286 (agy prompt dropped via stdin), #291 (frontend deprecated note). Recently active.
  - `housekeeping/`, `revival/`, `self-learn/`, `doubt-driven-development/`, `improve/`, `find-bugs/` — no PRs explicitly invoking them in the W33–W34 window, but `housekeeping` and `improve` are referenced in CLAUDE.md and used sporadically (per the dreaming convention).
  - `lib/`, `references/` — internal support dirs, not skills per se.
- Unused / candidate-for-pruning: **none with high confidence**. The `lib/` and `references/` directories are infrastructure; the rest are all referenced in CLAUDE.md or recent PRs.
- Suggest: no action. The W33 sweep already retired `security-reviewer/` from agents; no analogous skill retirement is pending.

### Agents (`.claude/agents/`)
- `bug-fixer.md` (6.4K), `ci-build-agent.md` (5.2K), `code-generator.md` (5.5K), `code-simplifier.md` (19.1K), `docs-maintainer.md` (5.4K), `go-security-reviewer.md` (22.1K), `pm-issue-writer.md` (7.6K), `static-analysis.md` (6.7K), `tech-lead.md` (10.6K), `test-generator.md` (8.5K)
- Note: `code-simplifier.md` (19.1K) and `go-security-reviewer.md` (22.1K) are notably larger than the others. `code-simplifier` is large because it embeds the full Go idioms / style guide; `go-security-reviewer` is large because it embeds the security checklist. Neither is bloated — both are reference material by design.
- `tech-lead.md` (10.6K) is the largest of the orchestration agents, expected given its review-criteria content.
- Overlapping responsibilities: **none obvious** after the W33 retirement of `security-reviewer.md`. `go-security-reviewer` is now the sole security agent.
- Suggest: no action.

## 6. Patterns I noticed

- **Sweep-with-sweep pattern.** The W33 work introduced a clean pattern: a Tech-Lead audit identifies 15 files needing frontend-staleness removal, ships as PR #341; the same audit's own out-of-scope findings (the README's `frontend` enum + dead `eslint-fix.cjs` hook) are *separately* tracked and not silently bundled. PR #343 then catches the residual artifacts the audit itself missed. This is the "verify-before-done" pattern applied at the meta-level, and it's working.
- **Two-tier verification in `/fix-review`.** The 3-model parallel pass + Claude arbiter structure is now mature: model verdicts are treated as confidence priors (not proof), and the arbiter routinely catches both false positives and false negatives. The W19-era "vote count ≠ verdict" warning is being honored — multiple recent PRs (#323, #340, #341) explicitly re-derive findings from source before confirming or dismissing.
- **Verification rituals baked into PR bodies.** Almost every recent PR body includes an explicit "Build, vet, and `go test -race -count=1 ./...` all pass (N tests, M packages)" line, plus a "manual verification" or "independent scan" line. This is the kind of self-documenting workflow that the W19 sweep was nudging toward; it's now the default.
- **`.claude/` governance is working.** The rule "no `.claude/` change without explicit approval" is being honored — even when both #341 and #343 *could* have bundled the out-of-scope items, they didn't. The deferred items are tracked in PR bodies, not in code, awaiting a separate decision. This is exactly the discipline the rule was designed to enforce.
- **No new debt symbols (⚡/⚖️/🏗️) in the W33–W34 window.** Every merged PR is either `chore`, `docs`, `fix`, or `feat`. The last `🏗️` was #323 (YAML registry); the last `⚖️` was #322 (RoleBased DevilsAdvocate); the last `⚡` was #325 (gitignore fix). The debt-level taxonomy is being applied but isn't dominating the commit stream — the codebase is in a *consolidation* phase, not a *growth* phase.
- **`sessions.db` is large (131.6 MB).** Not a rules issue, but the W19-era dreaming-prompt template doesn't account for this — `graphify` reads and other session-recall hooks may start hitting I/O pressure. Worth flagging to the W35 pass as a possible rotation or archival policy candidate.

## 7. What I couldn't tell (need manual review)

- **`sessions.db` size growth rate.** Is 131.6 MB a one-time growth from a long session, or steady accumulation? If steady, a rotation/archival policy (e.g. drop entries older than N weeks) might be worth proposing. The W35 pass could `ls -la` it across a week to see the trend, or `wc -l` to estimate rows.

  > [applied 2026-08-23: verified same-day growth 131.6MB -> 138MB (~565K rows) during /apply-dreaming triage -- real, not a one-time artifact of this report-writing session. No policy decided (needs a trend over 1-2 weeks first). gh issue #344 created to track the decision; no code change made.]

- **The two DEFER items from PR #341** (`frontend` enum in plans/README.md, dead `eslint-fix.cjs` hook) are tracked in PR bodies but not in any persistent `.claude/` index. If the user is doing the W34 `apply-dreaming` pass, surfacing these explicitly might help.

  > [applied 2026-08-23: confirmed already resolved -- both files verified absent from disk. Fixed in PR #343 in the same session that produced this report, before the report was read. No further action.]

- **Whether `graphify` self-learn Stop-hook (PR #324) is still firing.** It was the most error-prone PR of the window (3 confirmed findings, including a self-caught `grep -q '^['` bracket bug). If it's misbehaving in current sessions, the W35 pass should see the effect in `graphify-out/` mtimes or in `session-recall` results.

  > [manual-review-required 2026-08-23: mixed signal, not conclusive. `.claude/_patterns/mistakes.jsonl` has one entry since 2026-08-16; `wins.jsonl` has none since 2026-07-29, despite many substantial sessions in between (this session alone shipped 6+ PRs). Could be under-firing, or the hook's conservative false-positive avoidance correctly finding nothing loggable. Separately (not this dreaming report's original concern, but found during the same check): `graphify-out/graph.json` mtime is 2026-08-15 despite `~/.cache/graphify-rebuild.log` showing rebuild activity dated 2026-08-23 that never touched the live file -- the rebuild mechanism runs but the backups it produces aren't reaching the live graph. Both are external-tooling behavior (`graphify` binary, session-self-learn hook script) outside this repo's fix scope -- flagged, not fixed here.]

- **Whether `qwen3.5:cloud` / `minimax-m2.7:cloud` / `gemma4:31b-cloud` are all still alive.** Per the global CLAUDE.md rule about Ollama cloud-model retirements and the W33 §1 sweep, retired `-cloud` tags 404 silently. The recent fix-review comments consistently cite all three models, but I didn't run a live health check from this read-only pass. The W35 pass should `ollama list` (or the project's equivalent in `.claude/skills/fix-review/lib/`) to verify none have been silently retired since 2026-08-15 (the last `/fix-review` run).

  > [applied 2026-08-23: verified all three ALIVE via real `chat/completions` calls (HTTP 200, valid responses from each). Initial concern was a false alarm -- the `/v1/models` listing endpoint that `/fix-review`'s probe uses only returned 2 of 3 models, but `ollama list` showed all three as locally pulled, and direct calls to the two "missing" ones succeeded. `/v1/models` is an unreliable completeness signal; no retirement occurred, no config change needed.]
